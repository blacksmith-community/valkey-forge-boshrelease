<% if p('valkey.tls.enabled') -%>
<% if_p("valkey.tls.ca_key") do |key|
  if !key.match('PRIVATE KEY')
    key = key.gsub(/^(.{40}).*/, '\1...')
    raise "valkey.tls.ca_key '#{key}' does not look like a TLS private key"
  end
%>
<%= p('valkey.tls.ca_key') %>
<% end %>
<% end -%>
