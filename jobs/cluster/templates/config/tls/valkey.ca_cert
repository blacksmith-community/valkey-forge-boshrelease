<% if p('valkey.tls.enabled') -%>
<% if_p("valkey.tls.ca_cert") do |cert|
  if !cert.match('CERTIFICATE')
    cert = cert.gsub(/^(.{40}).*/, '\1...')
    raise "valkey.tls.ca_cert '#{cert}' does not look like a TLS certificate"
  end
%>
<%= p('valkey.tls.ca_cert') %>
<% end %>
<% end -%>
