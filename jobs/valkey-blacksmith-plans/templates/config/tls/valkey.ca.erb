#!/usr/bin/env ruby
<% if_p("valkey.tls.ca") do |cert|
  if ! cert.match('CERTIFICATE')
    cert = cert.gsub(/^(.{40}).*/, '\1...')
    raise "valkey.tls.ca '#{cert}' does not look like a TLS certificate."
  end
%>
<%= p('valkey.tls.ca') -%>
<% end -%>
